[package]
name = "tokio-select-examples"
version = "0.1.0"
edition = "2021"

[dependencies]
ansi_term = "0.12.1"
tokio = { version = "1.35.1", features = [
    "macros",
    "rt-multi-thread",
    "sync",
    "time",
] }
